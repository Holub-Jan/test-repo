name: testing content on push

on:
  workflow_call:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      token: ${{ secrets.GHTOKEN }}
      secret_test: ${{ secrets.TEST_TOKEN }}
    steps:
      - name: Test secret true
        if: env.secret_test == 'TEST'
        run: echo 'success'
      - name: Checkout
        uses: actions/checkout@v3.4.0
        with:
          repository: Holub-Jan/test-repo
          token: env.token

